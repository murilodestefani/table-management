import { render } from "@testing-library/react";
import { Tables } from "@components/Tables";

test("renderiza o componente Tables", () => {
  // Renderiza o componente Tables
  render(<Tables />);
});
